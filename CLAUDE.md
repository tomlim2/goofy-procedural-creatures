# CLAUDE.md

이 레포의 작업 규칙은 [`guidelines/README.md`](guidelines/README.md)에 있다.

**작업 시작 전에 `guidelines/README.md`를 반드시 읽는다.** 캐릭터(형태)·모션(움직임) 두 축, 카탈로그와
규칙 문서, 결정론(시드) 규칙, 검사 스크립트가 모두 거기 있다. 파츠를 고치면 [`guidelines/character/rules.md`](guidelines/character/rules.md),
모션을 고치면 [`guidelines/motion/rules.md`](guidelines/motion/rules.md)를 먼저 읽는다.

규칙을 고칠 때는 이 파일이 아니라 `guidelines/` 아래 문서를 고친다.
